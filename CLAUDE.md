# Project Context

This is Project 1 from my Agentic AI study plan: a command-line text
summarizer that calls the Claude API directly. I'm a non-developer
(LLC owner) — moderate comfort with running code, low comfort writing
it from scratch.

## Conventions
- Explain your reasoning before making changes.
- Keep changes small and reviewable — one function or one file at a time.
- Don't install new packages without asking first.

## Current goal
Building Project 1 from my study plan: a command-line text summarizer.
- Week 5: basic version — accept text input, send one Claude API call,
  print the summary. No error handling yet, just the core flow.
- Week 6: add guardrails — max input length check, try/except around
  the API call, and a rough cost estimate printed before it runs.

This is my first project using the actual Anthropic API directly
(not Claude Code) — the goal is learning the messages API shape.